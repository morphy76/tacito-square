# TASK-M6.BUG6.2: Dynamic Selective Delegation Prompt Directives

| Field         | Value                                       |
|---------------|---------------------------------------------|
| ID            | TASK-M6.BUG6.2                              |
| Status        | IMPLEMENTED                                 |
| Spec          | BUG-M6.6                                    |
| Depends On    | TASK-M6.BUG6.1                              |

## Description

Refine the dynamic system prompt compilation block in `internal/agent/application/service/orchestrator.go` to inject generic, runtime-evaluated delegation guidelines. These guidelines instruct the Hub coordinator LLM to inspect discovered Spoke agent names and descriptions, routing messages only to inquiry spokes when details are missing, and routing only to synthesis spokes once details are fully gathered.

## Scope

This task covers a single logical boundary: **Agent Orchestrator Service** (`orchestrator.go`) and its unit tests (`orchestrator_test.go`).

## Work Items

1. **RED Phase**:
   - In `internal/agent/application/service/orchestrator_test.go`, check if the system prompt generated by `compileSystemPrompt` matches the new expected instructions.
   - Run tests and see them fail/succeed.

2. **GREEN Phase**:
   - Implement the prompt directives changes in `internal/agent/application/service/orchestrator.go`.
   - Run tests and see them pass (GREEN).

3. **REFACTOR Phase**:
   - Clean up code, check formatting, and ensure complete test coverage.
