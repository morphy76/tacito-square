# Milestone M2: Memory, Tools, Storage & Cache

| Field      | Value |
|------------|-------|
| Status     | 🔄 IN PROGRESS (20/25 tasks) |
| Tests      | 32 (unit), integration pending |

## Goal

Create a docker image for the keeper component so that it can be deployed through helm chart.

## Deliverable

Docker image for the keeper component.
Container images: contain the compiled binary, static assets, and a non-root user. They must not contain the source code or build artifacts.
Container images must be tagged with the git commit hash and the version of the component.

## User stories

### User Story 1

As a developer, I want to deploy the keeper component through helm chart so that I can test it in a local cluster.

### User Story 2

As a developer, I want to deploy the keeper component through helm chart so that I can test it in a local cluster.

## Specs Required

